<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  typeAliases 를 쓰지 않는 프로젝트다. resultType / parameterType 은 FQCN 으로 쓴다.
  파라미터는 #{} 만 사용한다. ${} 는 SQL Injection 위험으로 금지.
-->
<mapper namespace="com.kb.tangtang.user.mapper.UserMapper">

    <!-- 조회 컬럼은 한 곳에서만 정의한다. 컬럼이 늘어도 고칠 데가 하나다. -->
    <sql id="userColumns">
        id, social_provider, provider_user_id, email, nickname, social_name, profile_image_key,
        name, status, difficulty_id, tutorial_seen_at, group_tutorial_seen_at,
        personal_mission_unlock_status
    </sql>

    <select id="findBySocialId" resultType="com.kb.tangtang.user.dto.UserDto">
        SELECT <include refid="userColumns"/>
          FROM tbl_user
         WHERE social_provider  = #{socialProvider}
           AND provider_user_id = #{providerUserId}
    </select>

    <insert id="insert"
            parameterType="com.kb.tangtang.user.dto.UserDto"
            useGeneratedKeys="true"
            keyProperty="id">
        <!--
          nickname 은 넣지 않는다. 가입 시점에는 NULL 이어야 "닉네임 온보딩 미완료" 로 판별된다.
          구글 이름은 social_name 에 들어간다. (DECISIONS.md 2026-08-11 닉네임 온보딩)
        -->
        INSERT INTO tbl_user (social_provider, provider_user_id, email, social_name, status, difficulty_id)
        VALUES (#{socialProvider}, #{providerUserId}, #{email}, #{socialName}, #{status}, #{difficultyId})
    </insert>

    <select id="findById" resultType="com.kb.tangtang.user.dto.UserDto">
        SELECT <include refid="userColumns"/>
          FROM tbl_user
         WHERE id = #{id}
    </select>

    <!--
      실명 갱신. 탈퇴/차단 사용자는 제외한다 — 상태가 ACTIVE 가 아닌 행을 고칠 이유가 없고,
      제외하면 updateName 이 0 을 돌려주므로 서비스가 그대로 404 로 응답한다.
    -->
    <update id="updateName">
        UPDATE tbl_user
           SET name = #{name}
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

    <!--
      닉네임 갱신. 온보딩(AU_03_01)과 마이페이지 수정(MY_01_03)이 같은 문장을 쓴다.
      중복 검사는 하지 않는다 — 닉네임 중복 허용이 팀 결정이라 UNIQUE 도 걸지 않았다.
      (DECISIONS.md 2026-08-11 닉네임 온보딩)
    -->
    <update id="updateNickname">
        UPDATE tbl_user
           SET nickname = #{nickname}
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

    <select id="findDifficultyIdByName" resultType="java.lang.Long">
        SELECT id
          FROM tbl_mission_difficulty
         WHERE difficulty_name = #{difficultyName}
    </select>

    <update id="updateDifficulty">
        UPDATE tbl_user
           SET difficulty_id = #{difficultyId}
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

    <!--
      튜토리얼 완료 시각. 완료(시각)와 「다시 보기」(NULL)를 한 문장으로 처리한다.

      대상 컬럼을 <choose> 대신 CASE 로 고른 이유:
        · 동적 SQL 분기 없이 평문 SQL 한 벌로 끝나 읽기 쉽다
        · target 은 바인딩 파라미터(#{})라 ${} 금지 규칙을 어기지 않는다
        · 대상이 아닌 컬럼은 자기 값을 다시 넣으므로 값이 보존된다

      jdbcType 을 명시하는 이유: seenAt 이 null 로 올 때(다시 보기) 드라이버가 타입을
      추론하지 못해 실패할 수 있다.

      ⚠ 'MAIN'·'GROUP' 은 TutorialType enum 의 이름이다. enum 을 고치면 여기도 고쳐야 한다.
    -->
    <update id="updateTutorialSeenAt">
        UPDATE tbl_user
           SET tutorial_seen_at = CASE WHEN #{target} = 'MAIN'
                                       THEN #{seenAt,jdbcType=TIMESTAMP}
                                       ELSE tutorial_seen_at END,
               group_tutorial_seen_at = CASE WHEN #{target} = 'GROUP'
                                             THEN #{seenAt,jdbcType=TIMESTAMP}
                                             ELSE group_tutorial_seen_at END
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

    <!--
      자격 여부를 요청 본문에서 받지 않고 같은 행의 relative_mission_qualified_at 으로 판정한다.
      (이슈 #315 (1) - 예전에는 클라이언트가 보낸 enoughData 를 그대로 믿어,
       false → true 를 연달아 쏘면 자격 없이 안내를 열 수 있었다)

      UNTRACKED + 자격 보유 → ELSE 로 떨어져 UNTRACKED 로 남는다. 기능이 붙기 전부터
      자격이 있던 사용자에게 「새로 열렸어요」를 띄우지 않기 위한 기존 동작이며 그대로 둔다.
    -->
    <update id="syncPersonalMissionUnlockStatus">
        UPDATE tbl_user
           SET personal_mission_unlock_status =
               CASE
                   WHEN personal_mission_unlock_status = 'UNTRACKED'
                        AND relative_mission_qualified_at IS NULL
                       THEN 'INSUFFICIENT'
                   WHEN personal_mission_unlock_status = 'INSUFFICIENT'
                        AND relative_mission_qualified_at IS NOT NULL
                       THEN 'PENDING'
                   ELSE personal_mission_unlock_status
               END
         WHERE id = #{id}
           AND status = 'ACTIVE'
    </update>

    <update id="acknowledgePersonalMissionUnlock">
        UPDATE tbl_user
           SET personal_mission_unlock_status = 'SEEN'
         WHERE id = #{id}
           AND status = 'ACTIVE'
           AND personal_mission_unlock_status = 'PENDING'
    </update>

    <!--
      프로필 이미지 키 갱신. profileImageKey 가 null 이면 미설정으로 되돌린다.
      #{} 는 null 을 그대로 NULL 로 바인딩하므로 분기가 필요 없다.

      status = 'ACTIVE' 가드는 형제 갱신문(updateName·updateNickname·updateTutorialSeenAt)과 같다.
      UserService.findActive() 는 이름과 달리 status 를 보지 않으므로(null 체크만 한다)
      **이 가드가 탈퇴·차단 계정의 쓰기를 막는 유일한 장치다.** 빼면 0행 대신 1행이 갱신된다.
    -->
    <update id="updateProfileImageKey">
        UPDATE tbl_user
           SET profile_image_key = #{profileImageKey}
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

    <!--
      회원 탈퇴. 행을 지우지 않는다 — tbl_user(id) 를 참조하는 FK 가 22개이고
      그 중 tbl_group_member·tbl_vote·tbl_group_challenge_daily_result 는
      **다른 사용자의** 그룹챌린지 이력과 얽혀 있다. 물리 삭제하면 남의 판결 기록이 깨진다.
      (tbl_user.status 주석 「물리 삭제 없음」 · DECISIONS.md 2026-08-13)

      provider_user_id 에 접미사를 붙여 uk_user_social 을 비운다 →
      같은 구글 계정으로 재가입하면 findBySocialId 가 미스가 나 신규 가입 경로를 탄다.
      VARCHAR(100) 이고 구글 sub 는 21자라 접미사를 붙여도 40자 안쪽이다.

      WHERE status = 'ACTIVE' 가 멱등성을 만든다 — 두 번 호출해도 접미사가 두 번 붙지 않는다.
      형제 갱신문(updateName·updateNickname·updateProfileImageKey)이 쓰는 조건과 같다.

      profile_image_key 는 NULL 로만 만들고 저장소 파일은 지우지 않는다.
      키를 잃으면 참조할 방법이 없고, 파일 I/O 를 트랜잭션에 넣으면 실패가 탈퇴를 롤백시킨다.
    -->
    <update id="withdraw">
        UPDATE tbl_user
           SET status            = 'WITHDRAWN',
               withdrawn_at      = #{withdrawnAt},
               email             = NULL,
               nickname          = NULL,
               social_name       = NULL,
               name              = NULL,
               phone             = NULL,
               birth_date        = NULL,
               gender            = NULL,
               profile_image_key = NULL,
               provider_user_id  = CONCAT(provider_user_id, '_withdrawn_', id)
         WHERE id     = #{id}
           AND status = 'ACTIVE'
    </update>

</mapper>

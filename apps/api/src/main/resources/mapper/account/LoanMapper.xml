<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kb.tangtang.account.mapper.LoanMapper">

    <sql id="columns">
        id, user_id, loan_no_encrypted, loan_no_masked, bank_name, bank_code, loan_type, loan_amount, balance,
        interest_rate, start_date, maturity_date, monthly_payment, next_payment_date
    </sql>

    <!--
      해제한 대출 숨김(#467). tbl_loan 에는 is_active 가 없어, 연결 해제는 tbl_connected_account 에
      같은 키(loan_no_encrypted = account_no_encrypted)의 is_active=0 행을 남기는 것으로 표현한다.
      행·거래는 그대로 두고(은행 계좌처럼 해제 전 이력은 보존) 읽을 때만 뺀다. 조회·합계·건수가 전부 이 조건을
      타야 관리 목록·자산 탭·동기화 범위가 같은 대출을 본다. 재연동 때 그 행이 지워지면 그대로 되살아난다.
    -->
    <sql id="notHidden">
        AND NOT EXISTS (
            SELECT 1
            FROM tbl_connected_account h
            WHERE h.user_id = l.user_id
              AND h.account_no_encrypted = l.loan_no_encrypted
              AND h.is_active = 0
        )
    </sql>

    <select id="findByUser" resultType="com.kb.tangtang.account.domain.Loan">
        SELECT <include refid="columns"/>
        FROM tbl_loan l
        WHERE l.user_id = #{userId}
        <include refid="notHidden"/>
        ORDER BY l.id
    </select>

    <update id="update" parameterType="com.kb.tangtang.account.domain.Loan">
        UPDATE tbl_loan
        SET bank_name         = #{bankName},
            bank_code         = #{bankCode},
            loan_no_masked    = #{loanNoMasked},
            loan_type         = #{loanType},
            loan_amount       = #{loanAmount},
            balance           = #{balance},
            interest_rate     = #{interestRate},
            start_date        = #{startDate},
            maturity_date     = #{maturityDate},
            monthly_payment   = #{monthlyPayment},
            next_payment_date = #{nextPaymentDate}
        WHERE user_id = #{userId}
          AND loan_no_encrypted = #{loanNoEncrypted}
    </update>

    <insert id="insert" parameterType="com.kb.tangtang.account.domain.Loan"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_loan
            (user_id, loan_no_encrypted, loan_no_masked, bank_name, bank_code, loan_type, loan_amount, balance,
             interest_rate, start_date, maturity_date, monthly_payment, next_payment_date)
        VALUES
            (#{userId}, #{loanNoEncrypted}, #{loanNoMasked}, #{bankName}, #{bankCode}, #{loanType}, #{loanAmount},
             #{balance}, #{interestRate}, #{startDate}, #{maturityDate}, #{monthlyPayment}, #{nextPaymentDate})
    </insert>

    <select id="sumBalanceByUser" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(l.balance), 0)
        FROM tbl_loan l
        WHERE l.user_id = #{userId}
        <include refid="notHidden"/>
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*)
        FROM tbl_loan l
        WHERE l.user_id = #{userId}
        <include refid="notHidden"/>
    </select>

</mapper>

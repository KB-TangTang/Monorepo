<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kb.tangtang.mission.mapper.MissionEvaluationMapper">

    <select id="findPendingAssignmentIds" resultType="java.lang.Long">
        SELECT id
          FROM tbl_user_mission_info
         WHERE assign_date = #{assignDate}
           AND result = 'PENDING'
         ORDER BY id
    </select>

    <select id="findPendingAssignmentIdsBefore" resultType="java.lang.Long">
        SELECT id
          FROM tbl_user_mission_info
         WHERE user_id = #{userId}
           AND assign_date &lt; #{beforeDate}
           AND result = 'PENDING'
         ORDER BY assign_date, id
    </select>

    <select id="lockPendingAssignment"
            resultType="com.kb.tangtang.mission.domain.MissionEvaluationTarget">
        SELECT assignment.id AS assignment_id,
               assignment.user_id,
               assignment.assign_date,
               assignment.target_value,
               COALESCE(SUM(
                   CASE WHEN mission_transaction.is_refund = 1
                        THEN -COALESCE(mission_transaction.refunded_amount, mission_transaction.amount)
                        ELSE mission_transaction.amount
                   END
               ), 0) AS current_amount
          FROM tbl_user_mission_info assignment
          JOIN tbl_mission_pool mission ON mission.id = assignment.mission_id
          LEFT JOIN tbl_transaction mission_transaction
            ON mission_transaction.user_id = assignment.user_id
           AND mission_transaction.category_id = mission.category_id
           AND mission_transaction.tr_date = assignment.assign_date
           AND mission_transaction.classification = 'CONSUMPTION'
           AND mission_transaction.direction = 'OUT'
           AND mission_transaction.is_excluded_from_summary = 0
         WHERE assignment.id = #{assignmentId}
           AND assignment.result = 'PENDING'
         GROUP BY assignment.id, assignment.user_id, assignment.assign_date, assignment.target_value
         FOR UPDATE
    </select>

    <update id="updateMissionResult">
        UPDATE tbl_user_mission_info
           SET result = #{result},
               evaluated_at = #{evaluatedAt}
         WHERE id = #{assignmentId}
           AND result = 'PENDING'
    </update>

    <insert id="increaseSuccessStreak">
        INSERT INTO tbl_streak_count
            (user_id, streak_count, longest_streak_count, last_check_date)
        VALUES
            (#{userId}, 1, 1, #{checkedAt})
        ON DUPLICATE KEY UPDATE
            longest_streak_count = GREATEST(longest_streak_count, streak_count + 1),
            streak_count = streak_count + 1,
            last_check_date = #{checkedAt}
    </insert>

    <insert id="resetStreak">
        INSERT INTO tbl_streak_count
            (user_id, streak_count, longest_streak_count, last_check_date)
        VALUES
            (#{userId}, 0, 0, #{checkedAt})
        ON DUPLICATE KEY UPDATE
            streak_count = 0,
            last_check_date = #{checkedAt}
    </insert>

</mapper>

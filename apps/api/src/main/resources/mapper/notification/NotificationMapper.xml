<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  typeAliases 를 쓰지 않는 프로젝트다. resultType 은 FQCN 으로 쓴다.
  파라미터는 #{} 만 사용한다. ${} 는 SQL Injection 위험으로 금지.

  ⚠ SQL 부등호는 &lt; 로 쓴다. 원시 < 를 쓰면 XML 파싱이 깨져
    sqlSessionFactory 생성이 실패하고 컨텍스트가 통째로 뜨지 않는다 (2026-08-06 실제 발생).
-->
<mapper namespace="com.kb.tangtang.notification.mapper.NotificationMapper">

    <sql id="columns">
        id, user_id, type, title, content, deep_link_url, is_read, created_at
    </sql>

    <insert id="insert" parameterType="com.kb.tangtang.notification.domain.Notification"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_notification (user_id, type, title, content, deep_link_url, is_read)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{deepLinkUrl}, 0)
    </insert>

    <select id="findPage" resultType="com.kb.tangtang.notification.domain.Notification">
        SELECT <include refid="columns"/>
        FROM tbl_notification
        WHERE user_id = #{userId}
        <if test="cursor != null">
            AND id &lt; #{cursor}
        </if>
        ORDER BY id DESC
        LIMIT #{size}
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM tbl_notification
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markRead">
        UPDATE tbl_notification
        SET is_read = 1
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllRead">
        UPDATE tbl_notification
        SET is_read = 1
        WHERE user_id = #{userId} AND is_read = 0
    </update>
</mapper>

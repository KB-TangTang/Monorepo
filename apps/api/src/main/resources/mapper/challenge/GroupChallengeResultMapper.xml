<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  typeAliases 를 쓰지 않는 프로젝트다. resultType 은 FQCN 으로 쓴다.
  파라미터는 #{} 만 사용한다. ${} 는 SQL Injection 위험으로 금지.

  ⚠ SQL 부등호는 &gt; · &lt; 로 쓴다. 원시 < 를 쓰면 XML 파싱이 깨진다.
-->
<mapper namespace="com.kb.tangtang.challenge.mapper.GroupChallengeResultMapper">

    <!--
      그룹 챌린지가 "소비" 로 세는 거래의 조건. 별칭은 거래 t · 그룹 g 로 고정한다.

      ⚠ 집계와 변론 화면의 거래 목록이 반드시 이 조각 하나를 같이 써야 한다. 조건이 갈리면
        "합계는 36,700원인데 목록을 더하면 31,700원" 같은 상태가 되고, 사용자는 자기가 왜
        기소됐는지 화면에서 확인할 수 없게 된다.

      ⚠ is_refund 는 여기서 거르지 않는다. 환불 행을 빼 버리면 원거래 금액이 그대로 남아
        환불받은 소비로 기소된다. 대신 합계 쪽에서 음수로 빼며, 이는 미션 판정·월간 리포트가
        쓰는 팀 공통 방식이다(MissionEvaluationMapper.xml:20 · MonthlyReportMapper.xml:15).
        같은 이유로 목록 조회도 환불 행을 함께 보여줘야 한다.

      category_id 가 NULL 인 그룹은 총소비 챌린지라 카테고리 조건이 사라진다.

      ⚠ 거래의 category_id 는 LLM 분류가 끝나야 채워진다(비동기). 분류 전 거래는 카테고리
        챌린지 집계에서 잠깐 빠진다. 다음 틱에 자동으로 반영되므로 기소가 늦어질 뿐 틀리지는 않는다.
    -->
    <sql id="consumptionFilter">
        AND t.classification = 'CONSUMPTION'
        AND t.direction = 'OUT'
        AND t.is_excluded_from_summary = 0
        AND (g.category_id IS NULL OR t.category_id = g.category_id)
    </sql>

    <!--
      표시용 닉네임. 소셜 로그인만 하고 닉네임을 안 정한 사용자가 빈칸으로 보이지 않게 한다.

      ⚠ 원본은 IndictmentMapper.xml:76-79 의 같은 이름 조각이다. 그쪽을 include 하지 못하는 이유는
        위 consumptionFilter 주석·아래 findTrialTransactions 주석과 같다 — ChallengeMapperXmlTest 가
        XML 을 한 장씩 독립된 Configuration 으로 파싱해 다른 namespace 의 조각을 찾지 못한다.
        식을 고칠 일이 생기면 두 파일을 함께 고친다.
    -->
    <sql id="displayNickname">
        COALESCE(NULLIF(TRIM(u.nickname), ''), u.social_name)
    </sql>

    <!--
      ★ 기간평가(PERIOD) 소비액 공식 — 이 주석이 원본이다. 아래 여섯 곳이 같은 식을 쓴다.

          GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)

        1) findOverLimitPeriod   (이 파일)              — 기간평가 기소 판정
        2) findMemberConsumption (이 파일)              — 그룹 상세 참여자 소비액
        3) findFinalConsumption  (이 파일)              — 최종 확정의 탈락 판정 · 부담금 (#172)
        4) findRankingActive     (이 파일)              — 명예 법정 진행 중 순위 (#173)
        5) findRankingClosed     (이 파일)              — 명예 법정 종료 후 표시용 소비액 (#173)
        6) IndictmentMapper.xml 의 exceededAmount 조각 — 재판 화면 초과액 · 변론 화면 총소비액

      ⚠ <sql> 조각 하나로 묶지 못했다. 상관 서브쿼리가 참조하는 바깥 별칭이 호출부마다 달라
        (r_end.user_id · m.user_id · i.user_id), 별칭을 넘기려면 ${} 를 써야 하는데 금지 대상이다.
        대신 **내부 별칭을 rp 로 통일**해 두었고 ChallengeMapperXmlTest 가 여섯 statement 의 SQL 에
        위 식이 문자 그대로 들어있는지 검사한다 — 한 곳만 고치면 테스트가 깨진다.

      ⚠ SUM(effective_amount) 로 바꾸지 말 것. effective_amount 는 생성 컬럼이라 GREATEST(...,0) 를
        **행마다** 건다. 환불은 음수 daily_amount 행으로 상쇄되는데(아래 upsertDailyResults 주석)
        그 음수가 0 으로 깎여 **환불받은 소비로 기소된다.** 합산한 뒤 한 번만 클램프해야 한다.

      ⚠ 거꾸로 GREATEST 를 빼도 안 된다. 환불이 소비보다 많은 기간은 합계가 음수가 되고
        화면의 게이지·달성률 계산이 음수로 무너진다.

      일일평가(DAILY) 갈래는 effective_amount 를 그대로 쓴다. 한 행이라 클램프 위치가 같다.
    -->

    <!--
      그룹 전원 · 하루치 재집계.

      LEFT JOIN 이라 거래가 없는 참여자도 0원 행이 만들어진다. 랭킹이 참여자 전원을 보여야 해서다.

      ⚠ ON DUPLICATE KEY UPDATE 에 verdict_deduction_amount 를 절대 넣지 않는다.
        넣으면 무죄 감액이 5분마다 0 으로 초기화되고 화면에는 흔적이 남지 않는다.
        effective_amount 는 생성 컬럼이라 애초에 쓸 수 없다.

      환불은 음수로 뺀다. 어제 산 것을 오늘 환불하면 오늘 daily_amount 가 음수가 될 수 있는데
      그대로 둔다 — effective_amount 가 GREATEST(..., 0) 로 막아 주고, 기간평가 SUM 에서는
      음수가 남아 있어야 환불이 제대로 상쇄된다. 여기서 0 으로 깎으면 기간 합계가 부풀어 오른다.

      target_count 는 '횟수' 라 환불 행을 세지 않는다(MissionCategoryAnalysisMapper.xml:50 과 동일).
      t.id IS NOT NULL 이 필요한 이유는 LEFT JOIN 미스 행에서 is_refund 가 NULL 이기 때문이다.

      idx_tx_user_date_cat (user_id, tr_date, ...) 를 탄다.
    -->
    <insert id="upsertDailyResults">
        INSERT INTO tbl_group_challenge_daily_result
            (group_id, user_id, challenge_date, daily_amount, target_count)
        SELECT m.group_id,
               m.user_id,
               #{challengeDate},
               COALESCE(SUM(CASE WHEN t.is_refund = 1
                                 THEN -COALESCE(t.refunded_amount, t.amount)
                                 ELSE t.amount
                            END), 0),
               COALESCE(SUM(CASE WHEN t.id IS NOT NULL AND t.is_refund = 0
                                 THEN 1 ELSE 0
                            END), 0)
          FROM tbl_group_member m
          JOIN tbl_challenge_group g ON g.id = m.group_id
          LEFT JOIN tbl_transaction t
                 ON t.user_id = m.user_id
                AND t.tr_date = #{challengeDate}
                <include refid="consumptionFilter"/>
         WHERE m.group_id = #{groupId}
         GROUP BY m.group_id, m.user_id
        ON DUPLICATE KEY UPDATE
            daily_amount = VALUES(daily_amount),
            target_count = VALUES(target_count)
    </insert>

    <!--
      일일평가 기소 대상.

      비교 기준이 effective_amount 다 — 무죄 감액으로 한도 이하가 된 참여자를 다시 기소하지 않는다.

      NOT EXISTS 는 성능용 필터다. 조회와 INSERT 사이에 다른 인스턴스가 끼어들 수 있어
      중복을 실제로 막는 것은 uk_ind_result UNIQUE 다(20260815_add_indictment_result_unique.sql).

      uk_gcdr_group_user_date (group_id, user_id, challenge_date) 는 선행 컬럼만 상등이라
      그룹 단위로 좁힌 뒤 날짜를 걸러 낸다. 그룹당 참여자가 최대 6명이라 문제되지 않는다.

      닉네임은 채팅 시스템 메시지 문구에만 쓴다(이슈 #355). 여기서 같이 뽑는 이유는 배치 루프
      안에서 따로 조회하면 기소 대상 수만큼 왕복이 늘기 때문이다 — 이 구문은 그룹당 한 번만 돈다.
    -->
    <select id="findOverLimitDaily" resultType="com.kb.tangtang.challenge.domain.IndictmentTarget">
        SELECT r.id             AS result_id,
               r.user_id        AS user_id,
               r.challenge_date AS challenge_date,
               r.effective_amount AS amount,
               <include refid="displayNickname"/> AS nickname
          FROM tbl_group_challenge_daily_result r
          JOIN tbl_challenge_group g ON g.id = r.group_id
          JOIN tbl_user u ON u.id = r.user_id
         WHERE r.group_id = #{groupId}
           AND r.challenge_date = #{challengeDate}
           AND r.effective_amount &gt; g.limit_amount
           AND NOT EXISTS (SELECT 1 FROM tbl_indictment i WHERE i.result_id = r.id)
         ORDER BY r.user_id
    </select>

    <!--
      기간평가 기소 대상. 챌린지가 끝난 뒤에만 호출된다(호출부가 today &gt; end_date 를 확인).

      기소는 end_date 행에 붙인다. 기간 중 어느 날이 한도를 넘겼는지 특정할 수 없기 때문이다.

      challenge_date BETWEEN start_date AND end_date 로 좁히는 이유: 배치는 기간 안의 날짜만
      기록하므로 지금은 없는 행이지만, 조건이 없으면 나중에 기간 밖 행이 한 줄이라도 생겼을 때
      합계가 조용히 틀어진다. 기소는 되돌릴 수 없는 처분이라 방어해 둔다.

      derived table 로 감싼 이유는 합계를 한 번만 계산하기 위해서다. WHERE 에 그대로 쓰면
      같은 서브쿼리를 두 번 적어야 한다.

      소비액 식은 위 「★ 기간평가(PERIOD) 소비액 공식」 주석을 따른다. 무죄 감액을 뺀 뒤 비교하므로
      일일평가가 effective_amount 로 비교하는 것과 취지가 같다 — 무죄로 한도 이하가 된 사람을
      다시 기소하지 않는다.

      닉네임은 findOverLimitDaily 와 같은 이유로 여기서 같이 뽑는다(이슈 #355). tbl_user 조인은
      derived table 안쪽에 둔다 — 바깥에 두면 r_end.user_id 가 이미 t 로 감싸여 있어 조인 키가 없다.
    -->
    <select id="findOverLimitPeriod" resultType="com.kb.tangtang.challenge.domain.IndictmentTarget">
        SELECT t.result_id, t.user_id, t.challenge_date, t.amount, t.nickname
          FROM (
              SELECT r_end.id             AS result_id,
                     r_end.user_id        AS user_id,
                     r_end.challenge_date AS challenge_date,
                     g.limit_amount       AS limit_amount,
                     <include refid="displayNickname"/> AS nickname,
                     (SELECT GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)
                        FROM tbl_group_challenge_daily_result rp
                       WHERE rp.group_id = g.id
                         AND rp.user_id = r_end.user_id
                         AND rp.challenge_date BETWEEN g.start_date AND g.end_date) AS amount
                FROM tbl_challenge_group g
                JOIN tbl_group_challenge_daily_result r_end
                  ON r_end.group_id = g.id
                 AND r_end.challenge_date = g.end_date
                JOIN tbl_user u ON u.id = r_end.user_id
               WHERE g.id = #{groupId}
          ) t
         WHERE t.amount &gt; t.limit_amount
           AND NOT EXISTS (SELECT 1 FROM tbl_indictment i WHERE i.result_id = t.result_id)
         ORDER BY t.user_id
    </select>

    <!--
      감액이 집계액을 넘어선 이상 행. 배치가 로그로만 남기고 고치지 않는다.
      (20260814_group_challenge_verdict_deduction.sql 이 #168 에 요구한 사항)

      ⚠ 일일평가(DAILY) 그룹만 검사한다. 기간평가는 **기간 전체의 감액**을 기소가 붙은 end_date 행
        한 줄에 적으므로 verdict_deduction_amount &gt; daily_amount 가 정상이다(#172 가 그렇게 기록한다).
        조건이 없으면 정상 데이터에서 이상 경고가 떠 로그를 못 믿게 된다.
        이 때문에 tbl_challenge_group JOIN 이 필요하다.

      ⚠ 감액이 0 인 행도 제외한다. daily_amount 는 환불이 소비보다 많은 날 음수가 되고
        (아래 upsertDailyResults 주석), 그러면 감액이 없는데도 0 &gt; -10000 이 참이 되어 경고가 뜬다.
        감액한 적이 없는 행은 감액이 넘칠 수 없다. (2026-08-18 수동 검증에서 실제로 걸렸다)
    -->
    <select id="findDeductionOverflow"
            resultType="com.kb.tangtang.challenge.domain.GroupChallengeDailyResult">
        SELECT r.id, r.group_id, r.user_id, r.challenge_date,
               r.daily_amount, r.verdict_deduction_amount, r.effective_amount, r.target_count
          FROM tbl_group_challenge_daily_result r
          JOIN tbl_challenge_group g ON g.id = r.group_id
         WHERE r.group_id = #{groupId}
           AND g.eval_type = 'DAILY'
           AND r.verdict_deduction_amount &gt; 0
           AND r.verdict_deduction_amount &gt; r.daily_amount
         ORDER BY r.user_id, r.challenge_date
    </select>

    <!--
      그룹 상세의 참여자별 소비액 (이슈 #169). 여기부터는 배치가 아니라 화면용 조회다.

      ⚠ CASE 두 갈래는 IndictmentMapper.xml 의 exceededAmount 조각과 같은 계산이다.
        한쪽만 고치면 "화면엔 한도 이하인데 재판에 올라가 있는" 참여자가 생긴다.
        일일평가는 그 날의 effective_amount, 기간평가는 위 「★ 기간평가(PERIOD) 소비액 공식」 주석의 식.
        둘 다 무죄 감액을 반영한 값이다.

      tbl_group_member 를 기준으로 LEFT 하지 않고 스칼라 서브쿼리 + COALESCE 로 0 을 채운다.
      집계 행이 아직 없는 참여자(첫날 오전 등)의 카드가 통째로 빠지면 인원이 모자라 보인다.

      정렬을 user_id 로 고정해야 화면의 카드 순서가 요청마다 흔들리지 않는다.
    -->
    <select id="findMemberConsumption"
            resultType="com.kb.tangtang.challenge.domain.GroupMemberConsumptionRow">
        SELECT m.user_id AS user_id,
               COALESCE(NULLIF(TRIM(u.nickname), ''), u.social_name) AS nickname,
               u.profile_image_key AS profile_image_key,
               CASE WHEN g.eval_type = 'PERIOD'
                    THEN COALESCE((SELECT GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)
                                     FROM tbl_group_challenge_daily_result rp
                                    WHERE rp.group_id = g.id
                                      AND rp.user_id = m.user_id
                                      AND rp.challenge_date BETWEEN g.start_date AND g.end_date), 0)
                    ELSE COALESCE((SELECT rd.effective_amount
                                     FROM tbl_group_challenge_daily_result rd
                                    WHERE rd.group_id = g.id
                                      AND rd.user_id = m.user_id
                                      AND rd.challenge_date = #{challengeDate}), 0)
               END AS amount
          FROM tbl_group_member m
          JOIN tbl_challenge_group g ON g.id = m.group_id
          JOIN tbl_user u ON u.id = m.user_id
         WHERE m.group_id = #{groupId}
         ORDER BY m.user_id
    </select>

    <!--
      변론 화면의 결산 구간 거래 목록 (이슈 #170).

      ⚠ 이 문장이 IndictmentMapper 가 아니라 여기 있는 이유는 consumptionFilter 조각 때문이다.
        namespace 를 붙이면 다른 XML 에서도 include 할 수 있지만, ChallengeMapperXmlTest 가
        XML 을 **한 장씩 따로** 파싱해 조각을 찾지 못하고 파싱이 실패한다. 조건을 복사하는 것보다
        조각 곁에 두는 편이 안전하다 — 복사하면 나중에 한쪽만 바뀐다.

      ⚠ 목록의 합계가 화면의 currentAmount 와 맞아야 한다. 사용자가 왜 기소됐는지 확인하는
        유일한 근거이고, 건별 실제부담금 입력의 상한 계산이 여기서 나온다. 그래서 조건을
        upsertDailyResults 와 같은 조각으로 공유하고 환불도 같은 규칙으로 음수화한다.

      tbl_challenge_group 조인은 consumptionFilter 가 g.category_id 를 보기 때문에 필요하다.
      조건이 g.id = #{groupId} 하나라 결과는 항상 1행이고 거래 행마다 붙는다.

      날짜 범위는 호출부가 정한다 — 일일결산은 challenge_date 하루, 기간결산은 그룹의
      start_date ~ end_date 다. 여기서 eval_type 을 다시 보고 갈래를 만들면 재판 상세와
      판단 주체가 둘이 된다.

      정렬은 날짜 → 시각 → id 다. tr_time 이 NULL 인 거래(원천에 시각이 없다)는 MySQL 이
      NULL 을 먼저 놓으므로 그 날 맨 위에 모인다. 화면이 시각 없이 그리면 되는 자리다.

      idx_tx_user_date_cat (user_id, tr_date, ...) 를 탄다.
    -->
    <select id="findTrialTransactions"
            resultType="com.kb.tangtang.challenge.domain.TrialTransactionRow">
        SELECT t.id            AS transaction_id,
               t.tr_date       AS tr_date,
               t.tr_time       AS tr_time,
               t.merchant_name AS merchant_name,
               CASE WHEN t.is_refund = 1
                    THEN -COALESCE(t.refunded_amount, t.amount)
                    ELSE t.amount
               END             AS amount,
               cat.category_name AS category_name,
               t.is_refund     AS is_refund,
               t.source_type   AS source_type,
               t.direction     AS direction,
               card.institution_name AS card_institution_name,
               acc.bank_name         AS account_bank_name
          FROM tbl_transaction t
          JOIN tbl_challenge_group g ON g.id = #{groupId}
          LEFT JOIN tbl_category cat          ON cat.id = t.category_id
          LEFT JOIN tbl_card card             ON card.id = t.card_id
          LEFT JOIN tbl_connected_account acc ON acc.id = t.account_id
         WHERE t.user_id = #{userId}
           AND t.tr_date BETWEEN #{fromDate} AND #{toDate}
           <include refid="consumptionFilter"/>
         ORDER BY t.tr_date, t.tr_time, t.id
    </select>

    <!--
      무죄 확정 시 인정된 감액 반영 (이슈 #172).

      ⚠ 대입이 아니라 누적이다. 기간평가는 기간 전체의 기소가 end_date 행 한 줄에 붙고
        일일평가도 같은 행이 다시 기소될 수 있다. 대입으로 쓰면 앞선 무죄의 감액이 사라진다.

      ⚠ effective_amount 는 SET 절에 넣지 않는다. STORED 생성 컬럼이라 쓰려고 하면 SQL 이 죽는다.
        이 컬럼만 바꾸면 DB 가 GREATEST(daily_amount - verdict_deduction_amount, 0) 로 다시 계산한다.
        (20260814_group_challenge_verdict_deduction.sql)

      감액이 집계액을 넘어도 막지 않는다 — 위 findDeductionOverflow 주석 참고.
    -->
    <update id="addVerdictDeduction">
        UPDATE tbl_group_challenge_daily_result
           SET verdict_deduction_amount = verdict_deduction_amount + #{amount}
         WHERE id = #{resultId}
    </update>

    <!--
      최종 확정 배치가 볼 참여자별 판정 재료 (이슈 #172).

      ⚠ PERIOD 갈래는 위 「★ 기간평가(PERIOD) 소비액 공식」 주석의 식을 그대로 쓴다.
        SUM(rp.effective_amount) 로 바꾸면 환불 음수 행이 행마다 0 으로 깎여 합계가 부풀고,
        기소를 만든 findOverLimitPeriod 와 값이 어긋난다 — "초과로 기소됐는데 완주" 가 생긴다.

      ⚠ DAILY 갈래는 반대로 effective_amount 를 합한다. 하루씩 따로 판정하는 평가라 클램프도
        하루 단위가 맞다. 이쪽은 탈락 여부를 lives_count 가 정하므로 이 값은 순위·표시용이다.
        내부 별칭을 rd 로 둔 것은 위 findMemberConsumption 의 DAILY 갈래와 같은 규칙이다.

      집계 행이 없는 참여자도 COALESCE 로 0 이 내려온다. 빠지면 그 사람의 final_* 가 NULL 로 남는다.
      정렬을 user_id 로 고정해 배치 로그를 두 번 돌렸을 때 비교할 수 있게 한다.
    -->
    <select id="findFinalConsumption" resultType="com.kb.tangtang.challenge.domain.GroupFinalizeRow">
        SELECT m.user_id     AS user_id,
               m.lives_count AS lives_count,
               CASE WHEN g.eval_type = 'PERIOD'
                    THEN COALESCE((SELECT GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)
                                     FROM tbl_group_challenge_daily_result rp
                                    WHERE rp.group_id = g.id
                                      AND rp.user_id = m.user_id
                                      AND rp.challenge_date BETWEEN g.start_date AND g.end_date), 0)
                    ELSE COALESCE((SELECT SUM(rd.effective_amount)
                                     FROM tbl_group_challenge_daily_result rd
                                    WHERE rd.group_id = g.id
                                      AND rd.user_id = m.user_id
                                      AND rd.challenge_date BETWEEN g.start_date AND g.end_date), 0)
               END AS charge_amount
          FROM tbl_group_member m
          JOIN tbl_challenge_group g ON g.id = m.group_id
         WHERE m.group_id = #{groupId}
         ORDER BY m.user_id
    </select>

    <!--
      명예 법정 랭킹 — 진행 중 그룹 (이슈 #173). 순위를 그 자리에서 계산한다.

      정렬(요구사항정의서 6.6)
        DAILY  : 남은 목숨 DESC → 누적 소비액 ASC
        PERIOD : 누적 소비액 ASC
      RANK() 라 동률은 공동 순위다(1, 1, 3). DENSE_RANK 로 바꾸면 공동 1위 둘 다음이 2위가 되어
      화면·요구사항(1, 1, 3)과 어긋난다.

      ⚠ 정렬 갈래는 <choose> 정적 분기다. 정렬 컬럼을 파라미터로 받으면 ${} 가 필요한데 금지
        대상이고, 호출부가 이미 읽은 eval_type 을 넘기므로 SQL 이 다시 읽을 이유도 없다.

      ⚠ rank 는 MySQL 8 예약어라 백틱이 필요하다 (전례: MissionScoreMapper.xml 의 ROW_NUMBER).

      소비액 CASE 두 갈래는 findFinalConsumption 과 같은 계산이다. 기간평가는 위
      「★ 기간평가(PERIOD) 소비액 공식」 주석의 식, 일일평가는 기간 전체 effective_amount 합.
      어긋나면 진행 중 순위와 종료 후 확정 순위(final_rank)가 이유 없이 달라진다.

      파생 테이블로 감싸는 이유: 윈도우 함수의 ORDER BY 가 집계 별칭(total_consumption)을
      참조해야 해서다. 같은 스칼라 서브쿼리를 SELECT 와 OVER 안에 두 번 적으면 한쪽만 고쳐진다.

      바깥 ORDER BY 의 user_id 는 공동 순위 안의 표시 순서 고정용이다. 없으면 새로고침마다
      같은 순위끼리 자리가 뒤바뀐다.
    -->
    <select id="findRankingActive" resultType="com.kb.tangtang.challenge.domain.GroupRankingRow">
        SELECT RANK() OVER (
                   <choose>
                       <when test="evalType == 'DAILY'">
                           ORDER BY base.lives_count DESC, base.total_consumption ASC
                       </when>
                       <otherwise>
                           ORDER BY base.total_consumption ASC
                       </otherwise>
                   </choose>
               ) AS `rank`,
               base.user_id           AS user_id,
               base.nickname          AS nickname,
               base.profile_image_key AS profile_image_key,
               base.lives_count       AS lives_count,
               base.total_consumption AS total_consumption
          FROM (
              SELECT m.user_id,
                     COALESCE(NULLIF(TRIM(u.nickname), ''), u.social_name) AS nickname,
                     u.profile_image_key,
                     m.lives_count,
                     CASE WHEN g.eval_type = 'PERIOD'
                          THEN COALESCE((SELECT GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)
                                           FROM tbl_group_challenge_daily_result rp
                                          WHERE rp.group_id = g.id
                                            AND rp.user_id = m.user_id
                                            AND rp.challenge_date BETWEEN g.start_date AND g.end_date), 0)
                          ELSE COALESCE((SELECT SUM(rd.effective_amount)
                                           FROM tbl_group_challenge_daily_result rd
                                          WHERE rd.group_id = g.id
                                            AND rd.user_id = m.user_id
                                            AND rd.challenge_date BETWEEN g.start_date AND g.end_date), 0)
                     END AS total_consumption
                FROM tbl_group_member m
                JOIN tbl_challenge_group g ON g.id = m.group_id
                JOIN tbl_user u ON u.id = m.user_id
               WHERE m.group_id = #{groupId}
          ) base
         ORDER BY `rank`, base.user_id
    </select>

    <!--
      명예 법정 랭킹 — 종료(CLOSED) 그룹 (이슈 #173). 확정값을 읽기만 한다.

      ⚠ 순위 재계산 금지가 이 구문의 존재 이유다. final_rank · final_outcome ·
        final_charge_amount 는 확정 배치(#172)가 write-once 로 남긴 값이고, 여기서 RANK() 를
        다시 돌리면 종료 후 환불·카테고리 재분류로 소비액이 바뀔 때마다 이미 사용자에게 보여 준
        순위가 뒤집힌다. 그래서 이 구문에는 **윈도우 함수 자체가 없다** —
        ChallengeMapperXmlTest 가 findRankingActive 와 나란히 모양을 검사한다.

      누적 소비액은 표시 전용이다. final_charge_amount 와 값이 다를 수 있는데(종료 후 환불 등)
      그것이 정상이다 — 순위·부담금은 확정값, 소비액 표시는 현재값.

      정렬은 저장된 final_rank 순. user_id 는 공동 순위 안의 표시 순서 고정용이다.
    -->
    <select id="findRankingClosed" resultType="com.kb.tangtang.challenge.domain.GroupRankingRow">
        SELECT m.final_rank           AS `rank`,
               m.user_id              AS user_id,
               COALESCE(NULLIF(TRIM(u.nickname), ''), u.social_name) AS nickname,
               u.profile_image_key    AS profile_image_key,
               m.lives_count          AS lives_count,
               m.final_outcome        AS final_outcome,
               m.final_charge_amount  AS final_charge_amount,
               CASE WHEN g.eval_type = 'PERIOD'
                    THEN COALESCE((SELECT GREATEST(SUM(rp.daily_amount - rp.verdict_deduction_amount), 0)
                                     FROM tbl_group_challenge_daily_result rp
                                    WHERE rp.group_id = g.id
                                      AND rp.user_id = m.user_id
                                      AND rp.challenge_date BETWEEN g.start_date AND g.end_date), 0)
                    ELSE COALESCE((SELECT SUM(rd.effective_amount)
                                     FROM tbl_group_challenge_daily_result rd
                                    WHERE rd.group_id = g.id
                                      AND rd.user_id = m.user_id
                                      AND rd.challenge_date BETWEEN g.start_date AND g.end_date), 0)
               END AS total_consumption
          FROM tbl_group_member m
          JOIN tbl_challenge_group g ON g.id = m.group_id
          JOIN tbl_user u ON u.id = m.user_id
         WHERE m.group_id = #{groupId}
         ORDER BY m.final_rank, m.user_id
    </select>

    <!--
      마지막으로 결산이 끝난 날짜 (이슈 #173). 일일평가 명예 법정의 「{날짜} 결산 반영」 부제.

      오늘 행은 5분 배치가 재집계하는 중이라 뺀다 — 어제까지가 "결산이 끝난" 날짜다.
      CURDATE() 대신 #{today} 를 받는 이유는 서비스의 Clock 주입과 기준 시각을 맞추기 위해서다.
      결산 행이 없으면 MAX 가 NULL 을 돌려주고, 화면이 날짜 부분을 생략한다.
    -->
    <select id="findLastSettlementDate" resultType="java.time.LocalDate">
        SELECT MAX(r.challenge_date)
          FROM tbl_group_challenge_daily_result r
         WHERE r.group_id = #{groupId}
           AND r.challenge_date &lt; #{today}
    </select>
</mapper>

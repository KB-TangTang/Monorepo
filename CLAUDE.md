# 탕탕 (지갑재판소) — Claude Code 진입점

이 프로젝트의 규칙 원본은 `AGENTS.md` 하나다. 아래를 임포트해 그대로 따른다.

@AGENTS.md

## Claude Code 전용 안내
- `apps/api` · `apps/web` · `db` 에서 작업할 때는 해당 폴더의 `AGENTS.md` 를 먼저 읽는다.
- 반복 작업(API 추가, 매퍼 작성, PR 전 점검 등)은 `.claude/skills/` 의 절차를 따른다.
- **규칙을 수정할 때는 이 파일이 아니라 `AGENTS.md` 를 고친다.** 이 파일은 포인터다.
